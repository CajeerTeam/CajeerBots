# Модули

Модуль — официальная функция платформы Cajeer Bots. Модули должны содержать общую бизнес-логику и не должны зависеть от конкретного мессенджера напрямую.

Каждый модуль описывается файлом `module.json`.
