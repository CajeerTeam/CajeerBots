# Безопасность runtime-нод

Минимальные требования distributed mode:

- `node_id` и `node_secret`;
- подпись команд;
- защита от replay;
- allowlist типов команд;
- таймаут выполнения команды;
- аудит административных действий;
- запрет произвольного удалённого кода.
