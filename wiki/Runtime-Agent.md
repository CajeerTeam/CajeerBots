# Runtime Agent

Runtime Agent — дополнительный исполнительный узел distributed mode.

Он хранит локальные секреты, подключается к внешним платформам через адаптеры, отправляет события в Core Server и исполняет только разрешённые команды.

Runtime Agent не должен исполнять произвольный удалённый Python-код.
