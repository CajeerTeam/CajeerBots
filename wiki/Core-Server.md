# Core Server

Core Server — дополнительный режим Control Plane для распределённой установки.

Он отвечает за API, маршрутизацию событий, политики, registry модулей/плагинов, права, аудит и команды для Runtime Agent.

В базовом local mode Core Server не нужен.
